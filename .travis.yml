language: node_js

node_js:
  - 0.10
  - 0.11

before_install:
  - npm install -g gulp
  - npm install -g karma
  - npm install -g karma-jasmine
  - npm install -g karma-chrome-launcher

script:
  - gulp ci --verbose --stack
